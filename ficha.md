# Фича: Караоке

## Описание

Караоке — образовательная фича для изучения языков через YouTube-видео с синхронизированными субтитрами. Пользователь вставляет ссылку на YouTube, система загружает субтитры, разбивает их на сегменты и показывает текст, подсвечивая текущую строку в такт воспроизведению. Интеграция со словарём позволяет быстро добавлять новые слова, идиомы и фразовые глаголы в личный словарь.

---

## Основные возможности

### 1. Загрузка и воспроизведение

- **Источник**: YouTube (ссылки форматов `youtube.com/watch?v=...`, `youtu.be/...`, `youtube.com/embed/...`)
- **Субтитры**: автоматическая загрузка через yt-dlp (ручные и автосгенерированные, формат SRT)
- **Плеер**: YouTube IFrame API
- **Режимы просмотра**:
  - `full` — видео + текст
  - `text-only` — только текст (режим караоке)
- **Полноэкранный режим**: при загрузке и выборе сохранённого видео автоматически включается полноэкранный режим караоке

### 2. Синхронизация текста

- Сегменты субтитров парсятся из SRT (время начала и конца)
- Текущий сегмент подсвечивается по времени воспроизведения
- Плавный автоскролл к активной строке
- Прогресс внутри сегмента (0–1) для визуализации

### 3. Управление воспроизведением

| Действие | Кнопка / клавиша |
|----------|------------------|
| Play / Pause | Пробел |
| Назад 5 сек | ← (ArrowLeft) |
| Вперёд 5 сек | → (ArrowRight) |
| Скорость вверх | ↑ (ArrowUp) |
| Скорость вниз | ↓ (ArrowDown) |
| Вкл/выкл звук | M |

- **Скорость воспроизведения**: 0.5x, 0.75x, 1x, 1.25x, 1.5x, 2x
- Громкость регулируется ползунком
- Переход к сегменту по клику на него

### 4. Сохранённые видео

- Список загруженных видео пользователя
- Загрузка сохранённого видео в плеер
- Удаление видео с подтверждением

### 5. Интеграция со словарём

#### Слова

- Клик по слову → запрос перевода/определения
- Добавление слова в словарь с контекстом (текст сегмента)
- TTS-озвучка слова

#### Идиомы

- Кнопка «Анализ идиом» → разбор текста песни (до ~20 идиом)
- Показ: фраза, дословный перевод, значение, примеры
- Добавление идиомы в словарь одним кликом

#### Фразовые глаголы

- Аналогичный анализ для фразовых глаголов
- Добавление в словарь с привязкой к видео

---

## Архитектура

### Backend (Node.js)

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/karaoke/transcribe` | POST | Загрузка субтитров YouTube, парсинг SRT, сохранение в БД |
| `/api/videos` | GET | Список видео пользователя |
| `/api/videos/:id` | GET, DELETE | Получить / удалить видео |
| `/api/vocabulary/add` | POST | Добавить слово в словарь |
| `/api/vocabulary/idioms/analyze` | POST | Анализ идиом по видео/тексту |
| `/api/vocabulary/idioms/add` | POST | Добавить идиому |
| `/api/vocabulary/phrasal-verbs/analyze` | POST | Анализ фразовых глаголов |
| `/api/vocabulary/phrasal-verbs/add` | POST | Добавить фразовый глагол |

### Процесс транскрипции (`/api/karaoke/transcribe`)

1. Проверка Bearer-токена (Supabase Auth)
2. Извлечение video ID из URL
3. Метаданные видео: `yt-dlp --dump-json --skip-download`
4. Субтитры: `yt-dlp --write-auto-subs --write-subs --sub-format srt --convert-subs srt --skip-download`
5. Парсинг SRT в сегменты `{ start, end, text }`
6. Сохранение в таблицу `user_videos`
7. Ответ: `videoId`, `title`, `thumbnail`, `segments`

### Frontend

- **Компонент**: `KaraokeTab.tsx` (`frontend/components/tabs/KaraokeTab.tsx`)
- **Использование**: вкладка «Караоке» в дашборде (`frontend/app/dashboard/page.tsx`)
- **Sidebar**: пункт «Караоке» с иконкой

### База данных (Supabase)

**Таблица `user_videos`:**

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | UUID | PK |
| user_id | UUID | FK → auth.users |
| video_url | TEXT | URL видео |
| video_type | TEXT | `youtube` \| `upload` |
| video_id | TEXT | YouTube video ID |
| title | TEXT | Название |
| transcription_text | TEXT | Полный текст |
| transcription_segments | JSONB | Массив `{ start, end, text }` |
| language | TEXT | Источник, напр. `youtube-subs` |
| created_at, updated_at | TIMESTAMPTZ | |

RLS: пользователь видит и изменяет только свои видео.

---

## Зависимости

- **yt-dlp**: установлен в системе (или `yt-dlp.exe` на Windows)
- **YouTube IFrame API**: подгружается через script
- **Supabase**: аутентификация и хранение

---

## Интеграция с дашбордом

- **Счётчик песен** (`karaokeCount`): число записей в `user_videos`
- **Быстрый доступ**: кнопка «Караоке» на главной
- **Недавняя активность**: действия с караоке показываются в ленте

---

## Файлы

| Файл | Назначение |
|------|------------|
| `frontend/components/tabs/KaraokeTab.tsx` | Основной UI караоке |
| `backend/src/index.js` | Эндпоинты karaoke, videos, vocabulary |
| `supabase/migrations/20240101000000_create_user_videos.sql` | Схема user_videos |
| `frontend/components/tabs/useDashboardMetrics.ts` | karaokeCount |
| `frontend/components/tabs/useRecentActivity.ts` | Активность по караоке |

---

# Фича: Собеседник (AI-агент)

## Описание

Собеседник — это голосовой и текстовый AI-партнёр для практики английского. Пользователь общается голосом (STT → LLM → TTS) или текстом в трёх режимах: **свободный диалог**, **ролевые сценарии** и **дебаты**. Есть подсказки ответов, панель переводчика и чат с ИИ для уточнений. Сценарии и темы дебатов можно создавать с помощью ИИ.

---

## Режимы работы

### 1. Свободный режим (Freestyle / Chat)

Неструктурированный разговор с AI без сценария. Пользователь сам задаёт тему и темп.

**Как работает:**

- Пользователь нажимает кнопку записи и говорит, либо печатает текст.
- Голос записывается, отправляется на `/api/agent/stt` (Whisper), распознаётся.
- Текст передаётся в `/api/agent/chat` (LLM, streaming NDJSON).
- Ответ озвучивается через `/api/agent/tts` (голосовые модели: Onyx, Nova, Ballad).

**Настройки свободного режима:**

- **Подсказки ответа** (режимы): естественно, проще, более нативно, вежливый вариант, без мата.
- **Сленг** (`slang_mode`): `off` — без сленга, `light` — лёгкий, `heavy` — активный.
- **18+ / нецензурная лексика:**
  - `allow_profanity` — разрешён ли мат в контексте;
  - `ai_may_use_profanity` — может ли ИИ использовать мат в ответах;
  - `profanity_intensity`: `light`, `medium`, `hard`.
- **Формальность** (`tone_formality`, 0–100): выше — вежливее и формальнее.
- **Прямота** (`tone_directness`, 0–100): выше — более прямой стиль.
- **Подсказка роли** (`role_hint`): например «собеседник в кафе», «коллега» и т.п.

Сессии сохраняются в `agent_sessions`, отображаются в истории и учитываются в аналитике.

---

### 2. Ролевые сценарии (Roleplay)

Диалог в заданной ситуации (заказ такси, отель, ресторан и т.д.) с чёткой целью и шагами.

**Как работает:**

1. Пользователь выбирает сценарий из каталога или «Моих сценариев».
2. Открывается брифинг: описание ситуации, роли, цели, шаги.
3. При необходимости — первая реплика персонажа (characterOpening) с озвучкой.
4. Пользователь отвечает голосом или текстом.
5. ИИ отвечает в образе персонажа, учитывая шаги и цель.
6. После каждого ответа ИИ помечает выполненные шаги (`completed_step_ids`).
7. Пользователь может запросить **подсказку ответа** — ИИ предлагает варианты фразы с учётом контекста и следующих шагов.
8. По завершении — «Отметить цель выполненной» → экран «Цель достигнута» → краткий фидбек и полезные фразы.
9. Опционально: **оценка речи** (`/api/agent/assess-speaking`) — баллы по критериям и рекомендации.

**Источники сценариев:**

- **Каталог** — встроенные сценарии по темам (такси, отель, магазин, ресторан и др.).
- **Мои сценарии** — пользовательские (созданные ИИ или скопированные из каталога).
- **Создание с ИИ** — см. ниже.

**Сленг и мат в сценариях:**

Каждый сценарий имеет свои настройки (`slangMode`, `allowProfanity`, `aiMayUseProfanity`, `profanityIntensity`). Они передаются в system prompt LLM и влияют на стиль диалога.

---

### 3. Дебаты (Debate)

Практика аргументации: пользователь и ИИ занимают противоположные позиции по теме.

**Как работает:**

1. Пользователь выбирает тему из каталога или вводит свою.
2. Выбирает позицию: «За» или «Против».
3. Выбирает сложность: easy, medium, hard.
4. Выбирает микроцели (аргументация, контраргументы и т.д.).
5. Настраивает, кто начинает: ИИ или пользователь.
6. Открывается брифинг с целью дебата.
7. Идёт поочерёдный обмен репликами.
8. Доступна **подсказка ответа** — ИИ предлагает реплику с учётом позиции и аргументов оппонента.
9. По завершении — фидбек в формате SBI (Situation, Behavior, Impact), сильные стороны, зоны роста, полезные фразы.
10. Опционально: **оценка речи** — баллы и рекомендации.

**Источники тем:**

- **Каталог** — готовые темы с уровнем сложности.
- **Своя тема** — произвольный текст.
- **Мои темы** — сохранённые пользовательские темы.

**Тема через ИИ:** при вводе своей темы вызывается `/api/agent/debate-topic-prepare` — валидация, нормализация, определение языка, рекомендованная сложность.

---

## Сленг и нецензурная лексика

| Параметр | Описание |
|----------|----------|
| `slang_mode` | `off` — нейтральная лексика; `light` — умеренный сленг; `heavy` — более живую речь |
| `allow_profanity` | Разрешена ли ненормативная лексика в контексте |
| `ai_may_use_profanity` | Может ли ИИ использовать мат в ответах |
| `profanity_intensity` | Степень: `light`, `medium`, `hard` |

Эти настройки задаются отдельно для:

- свободного режима;
- каждого ролевого сценария;
- каждого дебата (включая темы из «Моих»).

В промптах и подсказках ИИ явно учитываются `slang_mode` и `allow_profanity`, чтобы сохранять нужный стиль.

---

## Переводчик и чат с ИИ

### Панель переводчика (TranslatorPanel)

Открывается кнопкой в тулбаре Собеседника.

**Возможности:**

- Перевод текста EN ↔ RU (через LLM, `/api/agent/chat`).
- Перетаскиваемое и изменяемое по размеру окно.
- История переводов (`translator_history`), до 50 записей.
- Выделение слова/фразы в поле ввода или результата.
- **Добавление в словарь** — при выделении вызывается `analyzePhraseWithAI`: определение типа (word/idiom/phrasal_verb), получение перевода, примеров, транскрипции и сохранение в `user_vocabulary` / `user_idioms` / `user_phrasal_verbs`.

### Чат с ИИ (AiChatPanel)

Отдельный чат для любых вопросов и уточнений.

**Возможности:**

- Перетаскиваемое и ресайзируемое окно.
- История чатов (`ai_chat_sessions`), множество сессий.
- Создание, загрузка, удаление чатов.
- Сохранение сессий в БД при каждом ответе.

Оба окна открываются поверх Собеседника и могут использоваться во время разговора, сценария или дебата.

---

## Создание сценариев и тем с помощью ИИ

### Ролевые сценарии

- Endpoint: `POST /api/user-scenarios/generate`
- Вход: свободный текст (`prompt`), опционально — тема, место, роль пользователя, цель, уровень (A1–C1, easy/medium/hard), сленг и мат.
- ИИ возвращает JSON сценария: `systemPrompt`, `goal`, `goalRu`, `steps`, `characterOpening`, настройки стиля.
- Пользователь может отредактировать и сохранить через `createUserScenario` в «Мои сценарии».

### Темы дебатов

- Endpoint: `POST /api/agent/debate-topic-prepare`
- Вход: `topic_raw`, `locale`, `difficulty_mode` (auto/easy/medium/hard).
- Выход: `normalized_topic`, `status` (valid/warning/rejected), `detected_language`, `recommended_difficulty`, предупреждения и ошибки.
- Пользователь вводит тему вручную; ИИ валидирует и нормализует. Сохранение — в `user_debate_topics`.

---

## Подсказка ответа (Reply Hint)

`POST /api/agent/reply-hint` — по последней реплике ИИ предлагает вариант ответа пользователя.

Работает в режимах:

- **roleplay** — с учётом шагов, цели и контекста;
- **debate** — с учётом позиции, аргументов и микроцелей;
- **chat** — с учётом `freestyle_context` (формальность, прямота, микроцели).

Использует те же настройки сленга и мата, что и основной чат.

---

## Аналитика и прогресс

### Дашборд (useDashboardMetrics)

- **conversationMinutes** — общее время разговоров (оценка по числу сообщений).
- **todayMinutes**, **weekMinutes** — за сегодня и за неделю.
- **streakDays** — серия дней подряд с активностью (агент, ролевые, дебаты, словарь).

### Вкладка «Прогресс» (ProgressDashboard)

- **Ролевые сценарии**: завершения (`roleplay_completions`), оценки речи (`speaking_assessments`).
- **Дебаты**: завершения (`debate_completions`), оценки, SBI-фидбек.
- **Фильтры**: период, режим (roleplay/debate), источник (системный/личный).
- **KPI**: количество завершений, средние баллы.
- **Критерии**: fluency, grammar, vocabulary, pronunciation, coherence и др.
- **Рекомендации**: предложения следующих сценариев по фокусу.
- Переход к практике: клик по сценарию/дебату → `?tab=agent&from=progress&target=...`.

### Недавняя активность (useRecentActivity)

- События: «N мин с собеседником», ролевые сценарии, дебаты, караоке, словарь.
- Группировка по датам и типам.

---

## Архитектура (Собеседник)

### Backend

| Endpoint | Описание |
|----------|----------|
| `POST /api/agent/stt` | Распознавание речи (Whisper) |
| `POST /api/agent/tts` | Озвучка текста (TTS) |
| `POST /api/agent/chat` | Основной чат, streaming NDJSON |
| `POST /api/agent/reply-hint` | Подсказка ответа |
| `POST /api/agent/roleplay-feedback` | Фидбек по ролевому сценарию |
| `POST /api/agent/debate-feedback` | Фидбек по дебату (SBI) |
| `POST /api/agent/debate-topic-prepare` | Валидация и нормализация темы дебата |
| `POST /api/agent/assess-speaking` | Оценка речи |
| `POST /api/user-scenarios/generate` | Генерация сценария с ИИ |
| `GET/POST/PATCH/DELETE /api/user-scenarios` | CRUD личных сценариев |

### База данных

- `agent_sessions` — сессии с сообщениями, привязка к сценарию.
- `roleplay_completions` — завершения ролевых сценариев.
- `debate_completions`, `debate_sessions` — дебаты.
- `user_roleplay_scenarios` — личные сценарии.
- `user_debate_topics` — личные темы дебатов.
- `speaking_assessments` — оценки речи.
- `translator_history` — история переводов.
- `ai_chat_sessions` — чаты с ИИ.

### Frontend

- `AgentTab.tsx` — главный UI Собеседника.
- `RoleplayModeUI.tsx` — переключатель режимов и выбор сценариев.
- `PersonalScenariosUI.tsx` — каталог, создание с ИИ, мои сценарии.
- `DebateSetupUI.tsx` — выбор темы, настройка дебата.
- `TranslatorPanel.tsx` — переводчик.
- `AiChatPanel.tsx` — чат с ИИ (встроен в AgentTab).

---

# Фича: Аналитика (Прогресс)

## Описание

Аналитика — панель прогресса пользователя по практике устной речи. Показывает баллы, тренды, критерии оценки, последние сессии и рекомендации для повторения. Данные берутся из завершений ролевых сценариев и дебатов, а также из оценок речи (`speaking_assessments`).

---

## Источники данных

- **roleplay_completions** — завершения ролевых сценариев (до 500 записей).
- **debate_completions** — завершения дебатов.
- **speaking_assessments** — оценки речи (overall_score, criteria_scores, feedback).
- **user_vocabulary** — для подсчёта слов на повторение (`dueWordsNow`).
- **vocabulary_progress** — история повторений для будущей аналитики.

---

## Фильтры

### Период (PeriodFilter)

| Значение | Описание |
|----------|----------|
| `7d` | Последние 7 дней |
| `30d` | Последние 30 дней |
| `90d` | Последние 90 дней |
| `all` | За всё время |

Горячие клавиши: `1`–`4` для выбора периода.

### Режим (Mode)

| Значение | Описание |
|----------|----------|
| `roleplay` | Ролевые сценарии |
| `debate` | Дебаты |

Горячие клавиши: `R` — roleplay, `D` — debate.

### Тип контента (View)

| Значение | Описание |
|----------|----------|
| `system` | Системные сценарии/темы (из каталога) |
| `personal` | Личные сценарии/темы (созданные пользователем или ИИ) |

Горячие клавиши: `S` — system, `L` — personal.

---

## Структура экрана

### 1. ProgressHero

- **Средний балл** — кольцо (ScoreRing) из 10.
- **Практик** — число сессий за период.
- **Слабый критерий** — критерий с наименьшим средним баллом.
- **Дельта vs предыдущий период** — разница среднего балла (7/30/90 дней назад).
- **Streak** — количество дней подряд с хотя бы одной сессией (ролевые или дебаты).
- **График тренда** (TrendChart) — средний балл по дням или неделям, кликабельные точки.
- **Кнопки**: «Начать практику» → `/dashboard?tab=agent`, «Повторить слова» → `/dashboard?tab=dictionary`.

### 2. KPI-карточки (KpiCards)

| Карточка | Описание |
|----------|----------|
| Средний балл | Среднее overall_score за период, спарклайн, дельта vs пред. период |
| Сессии | Число завершений за период, спарклайн по дням/неделям, дельта |
| Лучший балл | Максимальный overall_score за период |

### 3. Критерии оценки (CriteriaOverview)

**Критерии** (по `speaking-assessment`):

- **Беглость** (fluency)
- **Лексика и грамматика** (vocabulary_grammar)
- **Произношение** (pronunciation)
- **Полнота и логика** (completeness)
- **Умение вести диалог** (dialogue_skills)

**Режимы отображения:**

- **Бары** — среднее по каждому критерию, min/max, число оценок, дельта (рост/снижение за вторую половину периода).
- **Радар** — полигон по пяти критериям, клик для выбора фокус-критерия.

Выбранный критерий влияет на рекомендации и сортировку сессий.

### 4. Последние сессии (RecentSessions)

Список завершений с:

- тип (🎭 ролевой / ⚔️ дебаты);
- название сценария или темы;
- балл (если есть оценка);
- дата (сегодня, вчера, N дн. назад и т.д.).

**Сортировка:** по дате, по баллу (↓/↑), по типу.

Клик по карточке → страница фокуса `/dashboard/progress/focus/{objectKey}?attempt={id}`.

### 5. Сценарии для повторения (RecommendedScenarios)

Рекомендации на основе **слабого критерия** (или выбранного вручную).

**Логика:**

- Берутся сессии с оценкой по этому критерию.
- Сортируются по возрастанию балла по критерию.
- Выводится до 8 карточек с приоритетом (high/medium/low) по уровню балла:
  - ≤ 4.5 — высокий приоритет («заметно проседает»);
  - ≤ 6.5 — средний («ниже целевого»);
  - выше — низкий («стабилизация»).

**На карточке:**

- название, приоритет;
- балл по критерию, общий балл;
- текст рекомендации, тренд за период (если ≥ 2 попыток);
- спарклайн истории баллов, число попыток;
- «Практиковать сейчас» → `/dashboard?tab=agent&from=progress&target={objectKey}`;
- «Открыть разбор» → страница фокуса.

---

## Страница фокуса (Focus)

URL: `/dashboard/progress/focus/[objectKey]` — детальный разбор по сценарию или теме дебата.

**objectKey:**

- `rp:{scenario_id}` — ролевой сценарий;
- `db:{topic_key}` — тема дебата (нормализованный ключ).

**Данные:**

- Все попытки по этому объекту за выбранный период.
- Для каждой попытки: дата, overall_score, criteria_scores, шаги (выполненные/нет), goal_attainment, strengths, improvements, coachComment.
- Связь ролевых с `agent_session_id`, дебатов с `debate_session_id` и `assessment_id`.

**Компоненты:**

- **FocusHero** — заголовок, средний/лучший балл, % выполнения шагов.
- **FocusAttemptSelector** — выбор попытки.
- **FocusCriteria** — баллы по критериям.
- **FocusProgressChecklist** — шаги и цели.
- **FocusFeedbackPanel** — сильные стороны, улучшения, комментарий тренера.
- **FocusTranscript** — расшифровка реплик пользователя (если есть `user_messages`).

Query-параметры: `period`, `mode`, `view`, `attempt` — сохраняются при переходах.

---

## Сопоставление оценок с завершениями

**Ролевые сценарии:** оценка привязывается к завершению по `scenario_id` и близости времени (до 48 часов).

**Дебаты:** оценка ищется по `assessment_id` в `debate_completions` или по `agent_session_id` / `debate_session_id`; при необходимости — по ближайшему по времени в пределах 48 часов.

---

## Аналитика на главном дашборде

На вкладке «Главная» (DashboardTab) выводятся сводные метрики:

- **conversationMinutes** — оценка минут по числу сообщений в `agent_sessions`.
- **todayMinutes**, **weekMinutes** — за сегодня и за текущую неделю.
- **streakDays** — серия дней подряд (агент, ролевые, дебаты, словарь).

---

## Архитектура (Аналитика)

### Данные (useProgressData)

Запросы к Supabase:

- `roleplay_completions` — id, scenario_id, scenario_title, scenario_level, completed_at, feedback, useful_phrase_*, completed_step_ids.
- `speaking_assessments` — для ролевых (scenario_id) и дебатов (по assessment_id / agent_session_id).
- `debate_completions` — темы, позиции, шаги, feedback, assessment_id.
- `user_vocabulary` — для dueWordsNow и mastery/times_practiced.
- `vocabulary_progress` — last_review_score.

### Фильтрация (useProgressFilters)

- periodStart — начало периода для фильтрации по дате.
- scopedRoleplayCompletions, scopedDebateCompletions — после фильтра по view (system/personal).
- scopedAssessments — оценки, связанные с отфильтрованными завершениями.
- isSystemDebate — проверка темы по каталогу DEBATE_TOPICS.

### Frontend

| Компонент | Назначение |
|-----------|------------|
| `ProgressTab.tsx` | Обёртка, рендерит ProgressDashboard |
| `ProgressDashboard.tsx` | Главный экран аналитики |
| `ProgressFilters.tsx` | Период, режим, тип контента |
| `ProgressHero.tsx` | Кольцо, тренд, streak, кнопки |
| `KpiCards.tsx` | Карточки KPI |
| `CriteriaOverview.tsx` | Бары и радар критериев |
| `RecentSessions.tsx` | Список последних сессий |
| `RecommendedScenarios.tsx` | Рекомендации для повторения |
| `focus/[objectKey]/page.tsx` | Страница фокуса по сценарию/дебату |

### База данных

- `roleplay_completions`
- `debate_completions`
- `debate_sessions` (fallback для старых данных)
- `speaking_assessments`
- `user_vocabulary`
- `vocabulary_progress`

---

# Фича: Словарь

## Описание

Словарь — персональная коллекция слов, идиом и фразовых глаголов пользователя. Хранит переводы, транскрипции, примеры, контексты из песен и поддерживает повторение по алгоритму SRS (интервальное повторение). Интегрирован с караоке (добавление по клику), переводчиком и собеседником.

---

## Три раздела словаря

### 1. Слова (Words)

Обычные слова из `user_vocabulary`.

**Поля:** word, translations, difficulty_level (A1–C2), part_of_speech, mastery_level (1–5), times_seen, times_practiced, contexts (из видео), next_review_at.

**Источники добавления:**

- Караоке — клик по слову в тексте песни → определение → «Добавить в словарь».
- Переводчик — выделение слова в поле ввода/результата → «Добавить в словарь» (через `analyzePhraseWithAI` для определения типа).
- Импорт — CSV, JSON, Anki.

### 2. Идиомы (Idioms)

Фразы из `user_idioms`: phrase, literal_translation, meaning, usage_examples, привязка к видео.

**Источники добавления:**

- Караоке — «Анализ идиом» по тексту песни (до ~20 идиом) → добавление одной кнопкой.
- Переводчик — выделение фразы → AI определяет тип (word/idiom/phrasal_verb) → добавление в соответствующий раздел.

### 3. Фразовые глаголы (Phrasal Verbs)

Аналогично идиомам, хранятся в `user_phrasal_verbs`.

**Источники добавления:**

- Караоке — «Анализ фразовых глаголов» по тексту.
- Переводчик — при определении типа `phrasal_verb`.

---

## Кэш определений (word_definitions_cache)

Общая таблица для всех пользователей. Хранит:

- definitions (переводы, источник);
- phonetic_transcription (IPA);
- part_of_speech;
- frequency_rank, difficulty_level;
- is_phrase (для идиом и фразовых глаголов);
- example_sentences.

При добавлении слова: сначала поиск в кэше; при отсутствии — запрос к LLM (getOrCreateWordDefinition), кэширование, списание баланса.

---

## Интерфейс DictionaryTab

### Режимы просмотра (viewMode)

- `words` — слова;
- `idioms` — идиомы;
- `phrasal-verbs` — фразовые глаголы.

### Фильтры

- **Поиск** — debounced (400 мс) по слову/фразе.
- **Уровень сложности** — A1–C2 или «Все уровни».
- **Категория** — выбор категории для фильтрации (для слов, идиом и фразовых глаголов отдельно).

### Сортировка (слова)

- По `difficulty_level`, asc/desc.

### Панель слова/идиомы/фразового глагола

- Слева — список с чекбоксами для множественного выбора.
- Справа — детали выбранного элемента:
  - слово/фраза;
  - переводы, транскрипция, часть речи, уровень;
  - примеры использования;
  - контексты из песен (с видео);
  - кнопка TTS (озвучка через `/api/tts`);
  - категории (назначение/отмена);
  - удаление.

### Статистика

- `total_words` — всего слов;
- `words_to_review` — на повторение (next_review_at ≤ now).

---

## Повторение (SRS)

### Список на повторение

- Endpoint: `GET /api/vocabulary/review-list`
- Источник: view `words_to_review_today` (next_review_at ≤ NOW, learning_status ≠ mastered).
- Лимит по умолчанию: 20 слов.

### Отправка результата

- Endpoint: `POST /api/vocabulary/review`
- Тело: `{ word, score }`, score от 0 до 1 (0 — не знаю, 1 — знаю).
- Логика:
  - score ≥ 0.7 — правильный ответ, сброс consecutive_incorrect;
  - score < 0.7 — неправильный, сброс consecutive_correct;
  - расчёт next_review_at через `calculateNextReview` (интервалы);
  - обновление mastery_level в `user_vocabulary`;
  - обновление vocabulary_progress (review_count, last_review_score, consecutive_*, next_review_at).

### Прогресс (vocabulary_progress)

- learning_status: new, learning, mastered, forgotten;
- review_count, last_review_score (0–1);
- consecutive_correct, consecutive_incorrect;
- next_review_at — для SRS.

---

## Категории

Таблица `vocabulary_categories`: name, description, color, icon. Связи many-to-many:

- `user_vocabulary_categories` — слова;
- `user_idioms_categories` — идиомы;
- `user_phrasal_verbs_categories` — фразовые глаголы.

View `vocabulary_categories_with_counts` — категории с количеством слов/идиом/фразовых глаголов.

**API:** GET/POST/PUT/DELETE `/api/vocabulary/categories`, POST `.../words/:id/categories`, `.../idioms/:id/categories`, `.../phrasal-verbs/:id/categories`.

---

## Экспорт и импорт

### Экспорт

- Endpoint: `GET /api/vocabulary/export`
- Параметры: format (csv, json, anki), view_mode, search, difficulty, category_id (или idiom_category_id, phrasal_verb_category_id).
- Форматы: CSV, JSON, Anki (CSV-карточки).

### Импорт

- Endpoint: `POST /api/vocabulary/import`
- Тело: `{ format, data }` — format: csv, json, anki; data — содержимое файла.
- Автоопределение Anki по заголовкам Front, Back в CSV.
- Ответ: imported, updated, total, errors[].

---

## Интеграции

### Караоке

- Клик по слову → `/api/vocabulary/define` (определение) → добавление через `/api/vocabulary/add` с video_id и context.
- Идиомы/фразовые глаголы — через analyze и add endpoints.

### Переводчик (TranslatorPanel)

- Выделение текста в input/output → `analyzePhraseWithAI` (LLM) → определение типа word/idiom/phrasal_verb.
- Добавление через `/api/vocabulary/add`, `/api/vocabulary/idioms/add`, `/api/vocabulary/phrasal-verbs/add` в зависимости от типа.

### Аналитика

- `dueWordsNow` — число слов на повторение (next_review_at ≤ now).
- Кнопка «Повторить слова» в ProgressHero ведёт на `/dashboard?tab=dictionary`.

---

## Backend API (словарь)

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/vocabulary/define` | GET | Определение слова (кэш или AI) |
| `/api/vocabulary/add` | POST | Добавить слово |
| `/api/vocabulary/list` | GET | Список слов с фильтрами |
| `/api/vocabulary/words/bulk-delete` | POST | Массовое удаление слов |
| `/api/vocabulary/word/:id` | DELETE | Удалить слово |
| `/api/vocabulary/export` | GET | Экспорт CSV/JSON/Anki |
| `/api/vocabulary/import` | POST | Импорт CSV/JSON/Anki |
| `/api/vocabulary/review-list` | GET | Слова на повторение |
| `/api/vocabulary/review` | POST | Результат повторения |
| `/api/vocabulary/idioms/analyze` | POST | Анализ идиом по видео/тексту |
| `/api/vocabulary/idioms/add` | POST | Добавить идиому |
| `/api/vocabulary/idioms/list` | GET | Список идиом |
| `/api/vocabulary/idioms/:phrase` | DELETE | Удалить идиому |
| `/api/vocabulary/idioms/bulk-delete` | POST | Массовое удаление идиом |
| `/api/vocabulary/phrasal-verbs/analyze` | POST | Анализ фразовых глаголов |
| `/api/vocabulary/phrasal-verbs/add` | POST | Добавить фразовый глагол |
| `/api/vocabulary/phrasal-verbs/list` | GET | Список фразовых глаголов |
| `/api/vocabulary/phrasal-verbs/:phrase` | DELETE | Удалить фразовый глагол |
| `/api/vocabulary/phrasal-verbs/bulk-delete` | POST | Массовое удаление |
| `/api/vocabulary/categories` | GET, POST | Список, создание категорий |
| `/api/vocabulary/categories/:id` | PUT, DELETE | Обновление, удаление |
| `/api/vocabulary/words/:id/categories` | POST | Назначить категории слову |
| `/api/vocabulary/idioms/:id/categories` | POST | Назначить категории идиоме |
| `/api/vocabulary/phrasal-verbs/:id/categories` | POST | Назначить категории фразовому глаголу |

---

## База данных (словарь)

| Таблица | Назначение |
|---------|------------|
| `word_definitions_cache` | Кэш определений (общий) |
| `user_vocabulary` | Слова пользователя |
| `vocabulary_progress` | Прогресс SRS по словам |
| `word_occurrences` | Вхождения слов в видео |
| `user_idioms` | Идиомы пользователя |
| `user_phrasal_verbs` | Фразовые глаголы пользователя |
| `vocabulary_categories` | Категории (name, color, icon) |
| `user_vocabulary_categories` | Связь слов и категорий |
| `user_idioms_categories` | Связь идиом и категорий |
| `user_phrasal_verbs_categories` | Связь фразовых глаголов и категорий |

Views: `user_vocabulary_with_details`, `words_to_review_today`, `user_vocabulary_stats`, `vocabulary_categories_with_counts`.

---

## Frontend

- **DictionaryTab.tsx** — основной UI: режимы, фильтры, списки, детали, TTS, категории, экспорт/импорт.
