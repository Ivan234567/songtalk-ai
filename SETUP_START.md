# Начало работы с Knowledge Graph Platform

## Шаги для запуска MVP

### 1. Настройка базы данных Supabase

1. Откройте Supabase Dashboard
2. Перейдите в SQL Editor
3. Скопируйте и выполните SQL из файла `supabase/schema.sql`
4. Это создаст все необходимые таблицы:
   - `nodes` - заметки/узлы
   - `node_links` - связи между узлами
   - `tags` - теги
   - `node_tags` - связь узлов с тегами

### 2. Настройка переменных окружения

#### Frontend (`.env.local` в папке `frontend/`):
```
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

#### Backend (`.env` в папке `backend/`):
```
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
PORT=3001
FRONTEND_URL=http://localhost:3000
```

### 3. Установка зависимостей

```bash
# В корне проекта
npm install

# Или отдельно
cd frontend && npm install
cd ../backend && npm install
```

### 4. Запуск проекта

```bash
# Запуск frontend и backend одновременно
npm run dev

# Или отдельно:
npm run dev:frontend  # Frontend на http://localhost:3000
npm run dev:backend   # Backend на http://localhost:3001
```

### 5. Первый запуск

1. Откройте http://localhost:3000
2. Вы будете перенаправлены на страницу входа
3. Создайте новый аккаунт через `/auth/register`
4. После регистрации вы попадете на главную страницу
5. Создайте свою первую заметку!

## Текущий функционал (MVP)

✅ **Реализовано:**
- Аутентификация (регистрация/вход/выход)
- Создание заметок (nodes)
- Просмотр списка заметок
- Удаление заметок
- Real-time обновления через Supabase Realtime
- Row Level Security (RLS) для защиты данных

🔄 **В разработке:**
- Редактирование заметок
- Связи между узлами
- Теги
- Визуализация графа

## Структура проекта

```
├── frontend/          # Next.js 14 приложение
│   ├── app/          # App Router страницы
│   │   ├── auth/     # Страницы аутентификации
│   │   └── page.tsx  # Главная страница
│   └── lib/          # Утилиты (Supabase клиент)
├── backend/          # Express.js API
│   └── src/          # Backend код
└── supabase/         # SQL схемы
    └── schema.sql    # Схема БД
```

## Следующие шаги

1. Добавить редактирование заметок
2. Реализовать связи между узлами
3. Добавить систему тегов
4. Создать простую визуализацию графа
5. Добавить поиск по заметкам
